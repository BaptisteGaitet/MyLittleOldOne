function init()
{
	screenWidth = window.innerWidth;
	screenHeight = window.innerHeight;
}

function onResize()
{
	screenWidth = window.innerWidth;
	screenHeight = window.innerHeight;
}

var screenWidth = 0;
var screenHeight = 0;

init();